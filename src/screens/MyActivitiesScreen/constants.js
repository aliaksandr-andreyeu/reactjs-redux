import i18n from '../../../i18n'

export const tabs = () => [i18n.t('activities.my_matches'), i18n.t('activities.my_invitations')]

// export const tabsEnum = tabs.reduce((prev, next, index) => {}, {});
